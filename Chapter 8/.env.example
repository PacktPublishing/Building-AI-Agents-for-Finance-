# Copy this file to `.env` and replace the placeholders with your real keys.
# Never commit the resulting `.env` file.

# Required for Stage 1 — the Investment Committee (investment_committee.py).
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Required ONLY for Stage 2 — the Adversarial Debate (adversarial_debate.py).
# The Bear analyst is intentionally on a different model family from the Bull;
OPENAI_API_KEY=your-openai-api-key-here
